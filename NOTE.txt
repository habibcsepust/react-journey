->React এ সবকিছু Component
->Component = JavaScript function → UI return করে
->JSX (HTML + JS together)
->Props (Data পাঠানো)
->State (Dynamic data)
->Event Handling
<button onClick={handleClick}>Click</button>
->List & map
const users = ['Ali', 'Hasan', 'Rahim'];
users.map(u => <li>{u}</li>)
->STEP 4: API / Backend connect (তোমার জন্য খুব easy)

তুমি যেহেতু ASP.NET Core API জানো 👏

useEffect(() => {
  fetch('https://api.example.com/users')
    .then(res => res.json())
    .then(data => setUsers(data));
}, []);
->STEP 5: CSS / Bootstrap
তুমি যেটা জানো সেটাই use করো:
Normal CSS
Bootstrap
Tailwind (later)
==============================================
--> Mapping er khetre jodi arrow(=>) sign er por second bace dei tahole obossoi return keyword use korte hobe.
like this below example.

function List() {
 console.log("uuid: "+uuidv4())
  return (
    <div className='task'>
        {todos.map((todo)=>{
            const {id, title, description} = todo;
            return <div key={id}>
                <h3>{title}</h3>
                <p>{description}</p>
            </div>
        })}
    </div>
  )
};


--> Mapping er khetre jodi arrow(=>) sign er por second brace na dei tahole obossoi return keyword use korte hobe na. like 
this below example.

data.map((item, index)=>(<CardHtml key={index} title={item.title} description={item.description} image={item.image} />))
